<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie Card</title>

    <style>
        /* =======================
           GLOBAL
        ======================= */
        body {
            min-height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: "Poppins", sans-serif;

            background:
                radial-gradient(circle at 20% 30%, rgba(255, 0, 0, 0.18), transparent 40%),
                radial-gradient(circle at 80% 70%, rgba(180, 0, 0, 0.15), transparent 45%),
                radial-gradient(circle at 50% 90%, rgba(120, 0, 0, 0.1), transparent 50%),
                #000;
        }

        /* =======================
           GLASS CONTAINER
        ======================= */
        .glass-card {
            padding: 18px;
            border-radius: 26px;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 16px;

            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(18px);
            border: 1px solid rgba(255, 255, 255, 0.15);
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.45);
        }

        /* =======================
           CARD
        ======================= */
        .card {
            position: relative;
            width: 260px;
            height: 390px;
            border-radius: 20px;
            overflow: hidden;
        }

        .card img {
            position: absolute;
            inset: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* dark gradient overlay */
        .card::before {
            content: "";
            position: absolute;
            inset: 0;
            background: linear-gradient(to top,
                    rgba(0, 0, 0, 0.85) 0%,
                    rgba(0, 0, 0, 0.45) 40%,
                    rgba(0, 0, 0, 0.15) 70%,
                    transparent 100%);
            z-index: 1;
        }

        /* =======================
           CARD INFO
        ======================= */
        .info {
            position: absolute;
            bottom: 0;
            z-index: 2;
            padding: 16px;
            width: 100%;
            color: #fff;
        }

        .info h3 {
            margin: 0 0 6px;
            font-size: 1.15rem;
            font-weight: 600;
        }

        .info p {
            margin: 4px 0;
            font-size: 0.85rem;
            opacity: 0.9;
        }

        .info p:last-child {
            font-size: 0.8rem;
            opacity: 0.75;
            cursor: pointer;
        }

        /* =======================
           CONTROLS
        ======================= */
        .controls {
            display: flex;
            gap: 22px;
        }

        .controls button {
            width: 54px;
            height: 54px;
            border-radius: 50%;
            border: none;
            cursor: pointer;
            font-size: 1.25rem;

            background: #8E1616;
            color: white;
            box-shadow: 0 8px 18px rgba(0, 0, 0, 0.4);
            transition: transform 0.2s ease;
        }

        .controls button:hover {
            transform: scale(1.12);
        }

        .back {
            color: #4da6ff;
        }

        .skip {
            color: #ccc;
        }

        .like {
            color: #ff4d6d;
        }

        .glass-card {
            perspective: 1200px;
        }

        .card-inner {
            position: relative;
            width: 300px;
            min-height: 520px;
            transform-style: preserve-3d;
            transition: transform 0.8s ease;
        }

        .glass-card.flipped .card-inner {
            transform: rotateY(180deg);
        }

        .card-face {
            position: absolute;
            inset: 0;
            backface-visibility: hidden;
            border-radius: 26px;

            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        /* Front keeps your glass effect */
        .card-front {
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(18px);
            border: 1px solid rgba(255, 255, 255, 0.15);
        }

        /* Back side */
        .card-back {
            background: #000;
            color: #fff;
            transform: rotateY(180deg);
            padding: 24px;
            text-align: center;
        }

        .card-back h2 {
            margin-bottom: 12px;
        }

        .card-back p {
            font-size: 0.9rem;
            opacity: 0.85;
        }

        .card-back .close {
            margin-top: 20px;
            padding: 10px 18px;
            border-radius: 20px;
            border: none;
            cursor: pointer;
            background: #8E1616;
            color: #fff;
        }

        .see-more {
            cursor: pointer;
            text-decoration: underline;
        }
    </style>
</head>

<body>

    <div class="glass-card" id="flipCard">
        <div class="card-inner">

            <!-- FRONT -->
            <div class="card-face card-front">
                <div class="card">
                    <img src="https://image.tmdb.org/t/p/w1280/2mxS4wUimwlLmI1xp6QW6NSU361.jpg" alt="Interstellar">

                    <div class="info">
                        <h3>Interstellar</h3>
                        <p>Sci-Fi • 2014 ⭐ 8.6</p>
                        <p>Action, Adventure, Science Fiction</p>
                        <p class="see-more">See more</p>
                    </div>
                </div>

                <div class="controls">
                    <button class="back">⟲</button>
                    <button class="skip">✕</button>
                    <button class="like">❤</button>
                </div>
            </div>

            <!-- BACK -->
            <div class="card-face card-back">
                <h2>Interstellar</h2>
                <p>
                    A team of explorers travel through a wormhole in space
                    in an attempt to ensure humanity’s survival.
                </p>

                <p><strong>Director:</strong> Christopher Nolan</p>
                <p><strong>Runtime:</strong> 169 min</p>

                <button class="close">← Back</button>
            </div>

        </div>
    </div>

    <script>
        const card = document.getElementById("flipCard");
        const seeMore = document.querySelector(".see-more");
        const closeBtn = document.querySelector(".close");

        seeMore.addEventListener("click", () => {
            card.classList.add("flipped");
        });

        closeBtn.addEventListener("click", () => {
            card.classList.remove("flipped");
        });
    </script>

</body>

</html>